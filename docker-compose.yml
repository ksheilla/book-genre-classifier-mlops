services:
  api:
    build: .
    expose:
      - "8000"
    deploy:
      resources:
        limits:
          memory: 1.5G

  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api